$(document).ready(function() {
	$('#s3-slider').unslider({
		nav: function(index, label) {
			// $('.s3-search-result-page-number .now').text(label)
		},
		autoplay: true,
		animation: 'vertical',
	})
})